{
  "name": "dev-sandbox",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": ["storefront"],
  "scripts": {
    "develop:codesandbox": "cp ./sandbox.env storefront .env && yarn develop",
    "develop": "yarn --cwd storefront watch"
  }
}
