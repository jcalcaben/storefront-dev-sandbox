{
  "name": "dev-sandbox",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": ["storefront"],
  "scripts": {
    "dev": "cp ./sandbox.env storefront/.env && yarn develop",
    "watch": "yarn --cwd storefront watch"
  }
}
